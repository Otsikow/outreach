{
    "name": "ai-outreach-ops",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*",
        "services/*"
    ],
    "scripts": {
        "dev": "npm run dev --workspaces",
        "build": "npm run build --workspaces",
        "postinstall": "npx prisma generate --schema=./packages/db/schema.prisma"
    }
}